{
  "name": "yarn3-test",
  "private": true,
  "packageManager": "yarn@3.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "yarn workspace client test && yarn workspace server test"
  },
  "dependencies": {
    "esbuild": "^0.12.28",
    "esbuild-jest": "0.5.0",
    "jest": "^27.3.1",
    "prettier": "^2.4.1"
  }
}
